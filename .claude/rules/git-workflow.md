# Git Workflow

## Commit Message Format

```
<type>: <description>
```

Types: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`

Examples:
- `feat: add dropout to attention mechanism`
- `docs: update educational guide with backprop visualization`
- `fix: correct softmax numerical stability`
